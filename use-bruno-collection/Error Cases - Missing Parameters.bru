meta {
  name: Error Cases - Missing Parameters
  type: http
  seq: 5
}

get {
  url: {{baseUrl}}/api/environmental-quality
  body: none
  auth: none
}

tests {
  test("should return 400 for missing parameters", function() {
    expect(res.getStatus()).to.equal(400);
    
    const error = res.getBody();
    expect(error).to.have.property("error");
    expect(error.error).to.include("Missing required parameters");
  });
}